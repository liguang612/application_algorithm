#include <iostream>

using namespace std;

int n, l1, l2, arr[1000001], _max = 0, _min = 0;

bool check(int x)
{
    int sum = 0;
    for (int i = 0; i < n - l1; i++)
    {
    }
}

int main()
{
    cin >> n >> l1 >> l2;

    for (int i = 1; i <= n; i++)
    {
        cin >> arr[i];
        _max += arr[i];
    }

    return 0;
}